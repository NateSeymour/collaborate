runtime: go
service: default
env: flex
runtime_config:
  operating_system: "ubuntu22"
  runtime_version: "1.21"
network:
  session_affinity: true
manual_scaling:
  instances: 1
env_variables:
  COLLABORATE_MODE: "production"
  COLLABORATE_PORT: ":8080"
  COLLABORATE_HOST: "collaborate.notyoursoftware.com"
  COLLABORATE_ENABLED_SERVICES: "realtime"

# TODO
# - Networking (connect all in subnet)
# - Instance size configuration

